Run the project with the command:
mvn spring-boot:run

Run the project tests with the command:
mvn test

Assumptions:
User can create tasks only with PENDING status
User can update task with any status (PENDING/DONE) and also change task with DONE status to PENDING status